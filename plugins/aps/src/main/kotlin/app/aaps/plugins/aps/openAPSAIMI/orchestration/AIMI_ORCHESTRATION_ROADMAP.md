
# AIMI orchestration — roadmap (tick context & pipeline)

Medical loop code must stay **behavior-identical** unless a change is explicitly reviewed. This document tracks **refactoring only** (structure, readability, test hooks), not medical logic.

**Dernière mise à jour doc :** **`runAdvancedPredictionsAndPredPipePrep`** + **`AimiAdvancedPredictionsPredPipePrep`** — **`applyAdvancedPredictions`**, **`sanitizePredictionValues`**, min BG composite, **`HypoThresholdMath.computeHypoThreshold`**, log **PRED_PIPE** ; mêmes **`sanity` / `minBg` / `threshold`** pour safety, Autodrive, **`minBgHypoComposite`**. P1 : commentaire Autodrive V3 raccourci.

## Réalisé

### Phase 1 — Comparator gate
- Préférence `BooleanKey.OApsAIMIAimiSmbComparatorEnabled` (défaut `false`) pour désactiver le comparateur SMB sans retirer le code.
- `AimiSmbComparator` sort tôt si la préférence est fausse.

### Phase 2 — Tick context (en cours)
- **`AimiTickContext`** (`orchestration/AimiTickContext.kt`) : regroupe les paramètres d’entrée de `determine_basal` (glucose, temp, IOB[], profil, autosens, repas, flags temps, UI, debug).
- **`runEarlyDetermineBasalStages(ctx)`** dans `DetermineBasalaimiSMB2` : même séquence qu’avant — `beginInvocation`, clear caches, pulse telemetry, reset flags, bruit CGM, `extraDebug`, `hydrateMealDataIfTriggered`, flags advisor/TDD, copie **`originalProfile`**, `enterPhase(BOOTSTRAP)`.
- **`AimiDetermineBasalEarlyTickState`** : `originalProfile`, `isExplicitAdvisorRun`, `tdd7P`, `tdd7Days` (valeurs figées après bootstrap numérique TDD).
- **Prélude post-bootstrap** : lectures alignées sur `ctx` de l’autopilot gestationnel / thyroïde jusqu’au multiplicateur basal unifié, `AimiDecisionContext`, init `RT`, SOS d’urgence, puis recalcul **local** de `flatBGsDetected` (override delta) — inchangé fonctionnellement.
- **Corps principal `determine_basal`** : la majorité des lectures d’entrée utilisent désormais `ctx` (IOB profiler, PKPD précoce, BYODA G6, tube advisor, branches MAXSMB pente/ plateau, **`runT3cBrittleBypassOrReturn`**, préparation signal / autosens, **`runAutodriveV3MultiVariableBranch`** puis fallback V2, cibles autosens, recomput PKPD, comparateur SMB, WCycle learning, auditeur, finalisation `decisionCtx` ISF). Les paramètres bruts ne subsistent que pour construire `AimiTickContext` et dans d’autres méthodes de la classe.
- **Harmonisation `ctx.currentTemp` / `ctx.mealData` / `ctx.microBolusAllowed` / `ctx.currentTime`** : tous les `setTempBasal` / `calculateRate` / comparaisons de TBR actif dans `determine_basal`, flux SMB (`enablesmb`, `finalizeAndCapSMB`, `executeSmbInstruction`), agrégats COB / pentes repas, NGR, moteur basal, PKPD meal path — sans toucher à `setTempBasal()` ni `applyLegacyMealModes()` (paramètres locaux inchangés).
- **P2 (amorcé)** : **`bootstrapPhysiologyAfterEarlyTick(ctx, tdd7Days)`** — regroupe autopilot gestationnel, résumé physiologique / IOB précoce, multiplicateurs basaux harmonisés, flag « confirmed high rise », module thyroïde ; appelé depuis `determine_basal` juste après `runEarlyDetermineBasalStages`.
- **P2 (suite)** : **`AimiTickDecisionRtBootstrap`** + **`buildDecisionContextInitRtSosAndFlatShadow(ctx)`** — construction `AimiDecisionContext`, `RT` initial, phase CONTEXT, raison `extraDebug`, SOS global, `logLearnersHealth`, reset WCycle / `lastProfile`, et **`flatBGsDetected` local** (override delta) ; `lastProfile` aligné sur **`ctx.profile`** (même référence que le paramètre `profile`).
- **P2 (suite)** : **`AimiRealtimePhysioIobBootstrap`** + **`runRealtimePhysioIobProfilerAndInsulinObserver(ctx, decisionCtx)`** — log activité RT, reset `maxSMB` / `maxSMBHB`, snapshot physio → `decisionCtx.adjustments.physiological_context`, `InsulinActionProfiler`, `iobActivityNow` membre, observateur insuline + logs PAI / PKPD_OBS.
- **P2 (suite)** : **`AimiGlucosePackLoadOutcome`** (`Abort` / `Continue`) + **`ensureWCycleAndLoadGlucoseStatusOrAbort(ctx, rT)`** — `ensureWCycleInfo()`, `glucoseStatusCalculatorAimi.compute(true)`, mêmes logs / early return / `ensurePredictionFallback` / `markFinalLoopDecisionFromRT` qu’avant ; le corps de `determine_basal` déstructure `glucoseStatus` et `AimiBgFeatures?` via un `when` exhaustif (smart-cast Kotlin).
- **P2 (suite)** : **`AimiT9PhysioPkpdTubeBootstrap`** + **`runT9PhysioEarlyPkpdAndTubeBootstrap`** — bloc T9 (log G6 lead), physio multipliers + trace, PKPD précoce + `cachedPkpdRuntime`, application physio / inflammation sur limites, echo TAP-G, tube advisor ; retourne `pumpAgeDays` + **`physioMultipliers`** (encore lus plus loin dans le tick : trajectoire / caps).
- **P2 (suite)** : **`AimiCombinedDeltaAndPeakTick`** + **`runCombinedDeltaByodaAndDynamicPeak`** — deltas récents, delta combiné, compensateur BYODA jour/nuit, `calculateDynamicPeakTime` vs `profile.peakTime` ; **volontairement avant** `Therapy` / `applyLegacyMealModes` (modes repas inchangés).
- **P2 (suite)** : **`AimiTickClockTirCarbGlucoseBootstrap`** + **`runTickClockMaxSmbTirCarbAndGlucoseCopy`** — `hourOfDay`, honeymoon, historique SMB, maxIOB/maxSMB (plateau/pente), NGR, snapshot TIR, `now`, glucides/tags, copie δ GS sur membres ; **stop avant** `Therapy` (horloges bfast/lunch/dinner/snack/highcarb inchangées).
- **P2 (suite)** : **`AimiTherapyExerciseGate`** (`Continue` / `ReturnEarly`) + **`runTherapyHydrateClocksAndExerciseLockoutGate`** — `Therapy`, CSV trim si delete, flags repas + runtimes, accélération/stabilité, `nightbis`, lockout sport → SMB 0, retours anticipés T3c / standard ; **juste avant** le bloc « MANUAL MEAL MODES » / `applyLegacyMealModes`.
- **P2 (suite)** : **`runT3cBrittleBypassOrReturn`** dans `DetermineBasalAIMI2` — extraction **mécanique** du bloc `BooleanKey.OApsAIMIT3cBrittleMode` : garde prébolus (historique bolus 20 min, lockout `internalLastSmbMillis`, plafond IOB vs `maxIob`), tick **shadow** Autodrive (DataLake / learner, sans commande), `applyAdvancedPredictions` + `applyTrajectoryAnalysis` avec **paramètres explicites** (`insulinActionState` depuis le bootstrap physio/IOB du tick, `physioMultipliers`, `pkpdRuntime`, `shortAvgDeltaAdj`, `originalProfile`, etc.), puis `executeT3cBrittleMode` comme avant. **`null`** si brittle désactivé (aucun effet). **Invariant d’ordre** : appel **immédiatement après** `applyLegacyMealModes` — le commentaire/code suppose que le prébolus legacy a déjà été traité en amont.
- **P2 (suite)** : **`runMealAdvisorDecisionOrReturn`** — encapsule l’appel **`tryMealAdvisor`** et, si `Applied`, la pose TBR, le direct send (cap 30 U), `logDecisionFinal` / `markFinalLoopDecisionFromRT`, **`return rT`**. **`null`** si fallthrough. Paramètres explicites (`modesCondition`, `isExplicitAdvisorRun`, `lastBolusTimeMs`, `hasRecentBolus45m`, `autodriveDisplay`, `iobData`, etc.) ; `hasRecentBolus45m` reste calculé **une fois** avant l’appel (réutilisé plus bas, ex. Drift Terminator). État classe inchangé (`adaptiveMult`, timers SMB internes).
- **Orchestration — Meal Advisor** (détail produit) :
  - **Placement** : dans le pipeline « DECISION », **après** **`trySafetyStart`** (si safety `Applied`, pas d’appel advisor).
  - **Logique métier** : toujours dans **`tryMealAdvisor`** (fenêtre carbs estimés, réfractaire 45 min, **`modesCondition || isExplicitTrigger`**, one-shot trigger).
  - **Si le advisor applique** : même contrat qu’avant — pas d’étape SMB/TBR aval qui écrase l’intention (retour immédiat via **`runMealAdvisorDecisionOrReturn`**).
- **P2 (suite)** : **`runHardBrakeLyraOrReturn`** — chute « falling decelerating » sous cible + marge 10 mg/dL → TBR 0 % 30 min, `lastSafetySource = HardBrake`, `logDecisionFinal` / `markFinalLoopDecisionFromRT`. **`null`** si condition fausse. **Invariant** : rester **avant** le bloc Autodrive V3 (ne pas nourrir la chute avant la logique MPC/V2).
- **P2 (suite)** : **`runAutodriveV3MultiVariableBranch`** — `BooleanKey.OApsAIMIautoDriveActive`, estimate repas 45 min, **`autodriveGater.shouldEngageV3`**, **`AutoDriveState.createSafe`** (SI PKPD ou `variableSensitivity`), **`autodriveEngine`** shadow off + active + **`tick`**, si commande safe → TBR + **`finalizeAndCapSMB`** + **`logDecisionFinal("AUTODRIVE_V3")`** ; retourne **`v3AppliedAction`** (bolus effectif ou TBR ≠ basal). Si engage mais commande unsafe/null : logs fallback **sans** remettre `lastAutodriveState` à WATCHING dans ce helper (comportement inchangé : reset **après** dans `determine_basal` si ≠ `ENGAGED` — cas limite `ENGAGED` + pas d’action V3 inchangé).
- **P2 (suite)** : **`runAutodriveV2FallbackBranch`** — `mealRising` / `contextFactor` / `contextPrefersBasal`, log lockout V3, reset `lastAutodriveState` → `WATCHING` si pas `ENGAGED`, **`tryAutodrive`** ou `Fallthrough`, branche **`Applied`** (TBR, SMB, cooldown / noop insulinReq).
- **P2 (suite)** : **`runPostHypoCompressionAndDriftTerminatorOrReturn`** — reçoit **`postHypoState`** déjà calculé (voir invariant 8). Compression → return `rT` ; Drift : **`shortAvgDelta` brut** (pas `shortAvgDeltaAdj`) dans **`isDriftTerminatorCondition`**, même logique `maxSMB` override.
- **P2 (suite)** : **`GlobalAimiBasalScheduleBootstrap`** + **`buildGlobalAimiBasalScheduleBootstrap`** — bloc unique après compression/Drift : `reason_maxsmb`, minute circadienne, caps pompe + basal profil validé, raisons CGM, override cible contexte, branches temp target / sensitivity ratio puis autosens, basal + WCycle, ajustement min/max/`target_bg` autosens ; `determine_basal` déstructure en `pumpCaps`, `profile_current_basal`, `basal`, `target_bg`, `min_bg`, `max_bg`, `sensitivityRatio`, `deliverAt`, `maxIobLimit` pour la suite du tick (**pas** de `lateinit` sur primitif local — `var sensitivityRatioLocal = 0.0`, toujours réécrit par le `if/else` après le `when`).
- **P2 (suite)** : **`AimiPostBasalBootstrapActivityVitals`** + **`runPostBasalBootstrapIobTickStepsAndHeartRate`** — juste après le bootstrap basal : écart IOB profiler vs système, `tick` / `minDelta` / `minAvgDelta`, ligne `consoleError` CR, fenêtres temps, pas (montre vs `StepService`), FC agrégée + ajustement ISF si tendance FC ; retour typé pour `tick` / deltas ; **inchangé** avant le bloc `tdd7Days` / `basalaimi`.
- **P2 (suite)** : **`AimiBasalAimiThroughPaiStageSnapshot`** + **`runBasalAimiTddCarbLimitsTirEarlyBasalAndPaiIsf`** — TDD/`basalaimi`, `ci`/`aimilimit`, grossesse+TIR, basal boost / early basal, PAI→`variableSensitivity`. Retour explicite de `timenow` / `sixAMHour` / `pregnancyEnable` (**même instant** qu’historiquement : après réglage `aimilimit`, avant branche TIR) pour **`BasalDecisionEngine.Input`** plus bas dans le tick ; évite de casser la portée locale après extraction.
- **P2 (suite)** : **`applyIsfBoundsAndPhysioMultipliersAfterEndoActivity`** — juste après **`applyEndoAndActivityAdjustments`** : `coerceIn` ISF, facteurs physio sur ISF / `max_daily_basal` / SMB (ou SMB=0 si lockout sport) ; retourne `variableSensitivity.toDouble()` pour réassigner **`sens`** comme avant (aucune `val` locale orpheline — mutations uniquement).
- **P2 (suite)** : **`AimiPkpdBgiDeviationAndTargetsStage`** + **`runPkpdPredictionsBgiDeviationAndNoisyTargetsStage`** — `effectiveSens`, `computePkpdPredictions`, `eventualBG`/`predictedBg`/`rT`, BGI + deviation 30m (même `round` 1-arg → **Int**), legacy eventual, ajustement cibles bruit si `adv_target_adjustments` ; retour explicite **`bgi`**, **`deviation`**, **`min_bg`/`target_bg`/`max_bg`** pour usage aval (CI ~L8501, raison ~L8560) sans casser la portée.
- **P2 (suite)** : **`runUamModelCalHypoGuardPostHypoAndSetPredictedSmb`** — `calculateSMBFromModel`, `shouldBlockHypoWithHysteresis`, rocket override, fallback 50 %, raison hypo (`minBg` **composite** hoisted ≠ `min_bg` cible), disambiguation **`postHypoState`** (rebound TBR / meal cap) ; met **`predictedSMB`** ; retourne **`modelcal`** pour logs + `executeSmbInstruction`.
- **P2 (suite)** : **`AimiSmbAdvisorLogAndExecutionStage`** + **`runSmbDecisionLogAdvisorOneShotAndExecuteInstruction`** — `hasPred`/`hyperKicker` (log uniquement), lecture **`isMealAdvisorOneShot`** puis branches prefs + cap `maxSMB`/`maxSMBHB`, log « SMB Decision », override DIA PKPD + `useLegacyDynamicsdia` (toujours inutilisé, `@Suppress`), **`executeSmbInstruction`** ; retour **`isMealAdvisorOneShot`** (valeur **avant** `put(false)` sur la préf, comme l’historique) + **`SmbInstructionExecutor.Result`** pour la suite du tick (PKPD guard, stacking, etc.).
- **P2 (suite)** : **`AimiPkpdGuardEndoRedCarpetSmbStage`** + **`runPkpdGuardEndoDampenRedCarpetAndCapSmb`** — `currentMaxSmb` (advisor bypass), guard PKPD + relief/debridage maxIOB, endo dampen, red carpet vs **`capSmbDose`**, ligne cap sur `rT.reason` ; retour **`smbToGive`** + **`intervalsmb`** (aucune autre `val` de ce bloc réutilisée avant `savedReason`).
- **P2 (suite)** : **`snapshotRtResetEnactmentFieldsRestorePredictionsAndPriorityCommands`** — copie `reason` / `predBGs` / `units` / `rate` / `duration`, réinitialise le buffer + métadonnées d’enactement (`deliverAt`, `targetBG`, ratios), **`ensurePredictionFallback`**, puis restaure commandes prioritaires ; **aucune** `val` exportée (tout est effet sur [rT]).

- **P2 (suite)** : **`AimiMealHyperBasalBoostOutcome`** (`ContinueWithOptionalRate` / `CompleteWithTempBasal`) + **`resolveMealHyperBasalBoostOutcome`** — même `when` que l’overlay basal repas / hyper / prudent / jeûne : les trois branches **max TBR 30 min** (advisor one-shot, snack, boost initial repas) deviennent **`CompleteWithTempBasal`** + `return` identique au lambda du tick ; le reste produit un **`Double?`** pour **`basalBoostApplied`** / `rT.rate` plus bas. Préférences **`meal_modes_MaxBasal`** vs **`autodriveMaxBasal`** inchangées ; lecture des flags repas / runtimes / **`bg`/`delta`** sur l’instance comme avant.
- **P2 (suite)** : **`AimiWCycleCsfCarbImpactStage`** + **`runWCycleIcCsfClampCiAndCarbImpactLogs`** — multiplicateur IC WCycle, CSF, plafond **`ci`** (champ **Float** mg/dL/5m vs `maxCI`), temps d’absorption résiduelle / COB / pic CI, pentes repas → **`slopeFromDeviations`** ; log CI/durée (l’impact courant est une variable locale **`ciCurrentImpact`** pour éviter l’ombre **`var ci: Double`** sur le champ). Retour **`csf`** + **`slopeFromDeviations`** pour **`CarbsAdvisor`** / **`HypoTools`** ; **`targetBg`** membre inchangé pour l’estimation glucides.
- **P2 (suite)** : **`AimiCarbsAdvisorEnableSmbSafetyStage`** + **`runCarbsAdvisorEnableSmbBasalHistoryAndSafetyStage`** — placé après WCycle/CSF, avant NGR / moteur basal : **`CarbsAdvisor.estimateRequiredCarbs`** avec **`targetBG = targetBg.toDouble()`** (invariant 9) ; **`HypoTools.calculateMinutesAboveThreshold`** ; prefs **`meal_modes_MaxBasal`** / **`autodriveMaxBasal`** ; **`mealModeActive`** ; **`enablesmb`** avec **`target_bg` local passé en **`targetBgSchedule`** ; ligne COB/Dev/BGI/ISF/CR/Target avec **`convertBG(targetBgSchedule)`** ; **`zeroSinceMin`** / **`minutesSinceLastChange`**, **`zeroBasalAccumulatedMinutes`**, raison eventual BG vs **`max_bg`** ; **`safetyAdjustment`** reçoit **`windowSinceDoseInt`** en paramètre explicite depuis le tick (fenêtre bolus — distinct de l’historique basal zéro) ; **`rT.isHypoRisk`** + **`HYPO_RISK_ALARM`** si besoin. Retour : prefs SMB, **`mealModeActive`**, compteurs basal, **`safetyDecision`** pour la suite (repas 0–30 min, NGR, **`computeMealHighIobDecision`**, **`applyBasalFloor`**, etc.).
- **P2 (suite)** : **`AimiPostSafetyMealNgrStageResult`** (`EarlyTempBasal` / `Continue`) + **`runPostSafetyMealFirst30NgrHeadroomBasalSmbStage`** — immédiatement après **`safetyDecision.stopBasal`** : détection repas + **`mealModeRuntimeToNullableMinutes`** (nullable + ms/s/min — **ne pas** remplacer par **`runtimeToMinutes(Long)`** sans revue) ; forçage TBR début repas 0–30 min (**`EarlyTempBasal`**) ; sinon **`nightGrowthResistanceMode.evaluate`** (IOB/COB via **`ctx`**) ; headroom **`maxIobLimit`** + **`this.maxIob`** ; boost basal / SMB (clamp **`maxSMB`** membre) ; **`Continue`** réinjecte **`isMealActive`**, **`runtimeMinValue`**, **`maxIobLimit`**, **`basal`**, **`smbToGive`**. **`BasalDecisionEngine.Input`** : runtimes snack/repas alignés sur **`mealModeRuntimeToNullableMinutes`** (même sémantique que l’ancienne fonction locale du tick).
- **P2 (suite)** : **`AimiCoreDecisionMaxIobGateResult`** (`ReturnTempBasal` / `ContinueSMBPath`) + **`runCoreDecisionMaxIobExceededTempBasalGate`** — **`computeMealHighIobDecision`**, puis **`AimiLoopTelemetry.enterPhase(CORE_DECISION)`**, puis si IOB boucle **`> maxIobLimit`** et pas relax : même branche TBR / **`applyBasalFloor`** / **`comparator.compare`** / **`logDecisionFinal("MAX_IOB")`** qu’avant (**`ReturnTempBasal`**) ; sinon **`ContinueSMBPath`** avec **`allowMealHighIob`** et **`mealHighIobDamping`** pour le chemin SMB + **`BasalDecisionEngine`**. Paramètre explicite **`loopIob`** (= **`iob_data.iob`**) pour ne pas confondre avec le champ **`iob`** (Float) de l’instance.
- **P2 (suite)** : **`runInsulinReqActivityRelaxAndMicrobolusStage`** — après **`ContinueSMBPath`** : construit **`insulinReq`** depuis **`smbToGive`**, clamp activité (membres **`activityProtectionMode`** / **`activityStateIntense`**, **`maxSMB`**), relax IOB repas, **`safetyDecision.bolusFactor`**, **`rT.insulinReq`**, log **`basalBoostApplied`** / **`basalBoostSource`** (arguments — définis plus haut dans le tick près de **`resolveMealHyperBasalBoostOutcome`**), branche microbolus + **`finalizeAndCapSMB`** (**`hypoThresholdMgdl`** = **`threshold`** local). Aucun **`return`** ; enchaîne sur **`runBasalDecisionEngineDecideStage`**.
- **P2 (suite)** : **`AimiBasalDecisionEngineStageBundle`** + **`runBasalDecisionEngineDecideStage`** — **`forcedMealActive`** recalculé comme avant ; **`BasalDecisionEngine.Input`** + **`Helpers`** + **`decide`** ; **`tickIobForEngine`** = **`iob.toDouble()`** (état tick), **`engineMaxIob`** = membre **`maxIob`** (≠ **`maxIobLimit`**). Drapeaux repas / **`mealModeRuntimeToNullableMinutes`** sur runtimes **instance** (**`snackrunTime`**, etc.) ; **`recentSteps5Minutes`**, **`fastingTime`**, **`sportTime`**, **`mealTime`**, … **membres**. **`basalLearner.process`** reste **après** le stage (ordre inchangé).
- **P2 (suite)** : **`AimiPostBasalEngineFinalizeBundle`** + **`runPostBasalEngineLearnersRtInstrumentationAndAuditorStage`** — **`basalLearner.process`** / UR learner / WCycle learning / bloc trajectoire ASCII ; résumé learners → **`setTempBasal`** (priorité TBR vs moteur inchangée) → **`comparator`** → clamp rate ; **`learnersInfo`** + instrumentation RT (**`RtInstrumentationHelpers`**) ; **`auditDecision`** + mutation **`finalResult`** en callback (**⚠️ async**). Entrées bundle : **`ctx`**, **`flatBGsDetected`**, **`pkpdRuntime`**, **`tdd7Days`**, **`intervalsmb`**, **`basalDecision`**.
- **P2 (suite)** : **`runAimiSnapshotMedicalJsonAndHormonitorExportStage`** — snapshot médical (**`decisionCtx`**) ; append **JSONL** ; phase **EXPORT** ; **`hormonitorStudyExporter`** (physio, TIR, TDD) — **I/O disque** ; erreurs → **`consoleError`** uniquement (**⚠️** ordre vs callback auditor inchangé ; KDoc explicite sur **état capturé avant** verdict async).
- **P2 (suite)** : **`runTrajectoryTightSpiralSafetyBridge`** — après **`applyTrajectoryAnalysis`**, avant **`applyContextModule`** : si dernière analyse = **`TrajectoryType.TIGHT_SPIRAL`**, fraction basale + CGate / repas, mutation **`rT`** / **`lastSafetySource`** / **`logDecisionFinal`** ; **pas** de **`return`** (LGS aval inchangé). Paramètre explicite **`physioMultipliers`** (local du tick, pas membre classe). Import **`TrajectoryType`**.
- **P2 (suite)** : **`runTddRatesAndIsfFusionAfterContext`** — après **`applyContextModule`** : dérivés **`tdd*PerHour`**, fusion **`fusedIsf`** / variable / profil, **`variableSensitivity`**, logs fusion + **`IsfTddProvider.set`** ; retour **`sens`** pour **`applyAdvancedPredictions`** (réassignable plus bas, ex. endo/physio). **Sync uniquement** (inchangé vs bloc inline).
- **P2 (suite)** : **`AimiAdvancedPredictionsPredPipePrep`** + **`runAdvancedPredictionsAndPredPipePrep`** — après micro-boluses dynamiques : **`applyAdvancedPredictions`** puis sanity / seuil hypo / **PRED_PIPE** ; déstructure en **`sanity`**, **`minBg`** (composite, ≠ **`min_bg`** schedule), **`threshold`** pour **`trySafetyStart`**, V3/V2, drift, UAM hypo composite. Import **`PredictionSanityResult`**.
- **P1 (doc, même commit)** : bannières ASCII / doublons « FCL » dans le tronçon trajectoire → contexte → TDD/ISF → predictions → safety ; une ligne de garde-fou sur l’ordre safety / advisor ; **`profileISF_raw`** supprimé (dead code). Tronçon Autodrive V3 : bannière « Super-iLet » remplacée par renvoi KDoc.

### Branche / build
- Travail sur `feature/aimi-phase2-tick-context`.
- Cible de compilation : `:plugins:aps:compileFullDebugKotlin`.

## Invariants à ne pas casser

1. **Références partagées** : `ctx.profile`, `ctx.mealData`, etc. pointent vers les **mêmes instances** que les paramètres de `determine_basal`. Une mutation en place du profil ou des repas reste visible via `ctx` — ce n’est pas une copie profonde.
2. **`flatBGsDetected` est ré-ombrelé** : après `val flatBGsDetected = if (...)`, le reste de la fonction doit utiliser le **`flatBGsDetected` local** (override delta), **pas** `ctx.flatBGsDetected` (valeur brute d’entrée).
3. **Ordre des effets** : tout déplacement de code doit préserver l’ordre des effets de bord (caches, telemetry, hydration repas, phases loop).
4. **T3c brittle** : ne pas déplacer **`runT3cBrittleBypassOrReturn`** avant **`applyLegacyMealModes`** (ni le fusionner avec le gate therapy) sans revue — le garde-fou prébolus et les logs restent cohérents seulement si les modes repas manuels / prébolus legacy ont déjà tourné.
5. **Meal Advisor** : garder **`trySafetyStart` avant** **`runMealAdvisorDecisionOrReturn`** (qui appelle **`tryMealAdvisor`**) ; si appliqué, **`return rT`** reste dans ce helper — ne pas réordonner pour laisser passer SMB standard par-dessus. **`modesCondition`** et **`isExplicitAdvisorRun`** inchangés vs fenêtres legacy.
6. **Hard Brake Lyra** : ne pas déplacer **`runHardBrakeLyraOrReturn`** **après** Autodrive V3 sans revue produit — la motivation est d’éviter basal/SMB agressifs quand la chute ralentit encore sous la cible.
7. **Autodrive V3** : appeler **`runAutodriveV3MultiVariableBranch`** **après** Hard Brake, **avant** le calcul `mealRising` / **`tryAutodrive`** (V2) — le booléen `v3AppliedAction` doit continuer à **verrouiller** V2 quand une action V3 a été appliquée. Ne pas réordonner pref/gater/tick par rapport au flux documenté dans le KDoc de la méthode (effets sur `lastAutodriveState` / moteur).
8. **Post-hypo** : **`classifyPostHypoState`** doit rester **une seule fois** par tick **après** V2 (qui peut muter `reason`) et **avant** compression/Drift ; le **`postHypoState`** est **réutilisé** plus loin pour l’ajustement SMB — ne pas le recalculer dans un helper fermé sans exposer le résultat au scope de `determine_basal`.
9. **CarbsAdvisor vs cibles** : **`CarbsAdvisor.estimateRequiredCarbs`** (bloc après WCycle/CSF) doit continuer à utiliser **`targetBG = targetBg.toDouble()`** (champ instance — objectif loop / temp target, déjà aligné avec PKPD, trajectoire, autodrive, etc.) — **pas** le remplacer par le local **`target_bg`** (milieu de bande schedule + ajustements PKPD/noisy) sous prétexte de « cohérence » avec **`enablesmb`** ou la ligne COB : ce sont deux définitions volontairement différentes ; un changement ici est une **décision produit**, pas un refactor mécanique. (**`HypoTools.calculateMinutesAboveThreshold`** n’utilise pas la cible glycémique, seulement BG / pente / seuil fixe.)
10. **Pas / FC physio (Unified + log)** : **`HealthContextRepository.fetchSnapshot()`** alimente le snapshot affiché dans le débogage ; **`getDetailedLogString()`** doit déclencher un **`fetchSnapshot()`** hors thread UI pour éviter un texte obsolète (0 pas / HR 0) alors que les tuiles AAPS sont à jour. **`UnifiedActivityProviderMTR.getStepsTotalSince`** : pour les fenêtres ~5/10/15/30/60/180 min, utiliser les champs **`steps5min`…`steps180min`** de la **dernière** ligne **[SC]** fraîche (même sémantique que la sync HC / téléphone) ; l’ancienne somme des seuls **`steps5min`** par bucket **sous-estimait** les totaux 15/60 min quand peu d’enregistrements tombent dans la fenêtre — écart possible de l’ordre de **~1000+ pas** vs agrégateurs « journée » si les définitions (fenêtre glissante vs total journée) diffèrent toujours.

## Reste à faire (priorité suggérée)

| Priorité | Tâche | Risque |
|----------|--------|--------|
| P1 | Finition : commentaires dupliqués **hors** tronçon post-context (fait ici pour trajectoire / pipeline décision) | Très faible |
| P2 | ~~étapes bootstrap + T3c + advisor + Hard Brake + Autodrive V3/V2 + compression/Drift~~ ; ~~bootstrap basal global (`buildGlobalAimiBasalScheduleBootstrap`)~~ ; suite : reste du **SMB / basal** aval dans le tick si encore monolithique, puis **pipeline typée** | Moyen |
| P3 | Introduire une **pipeline** typée (sealed `AimiTickPhase` ou séquence de stages) derrière la même API publique `determine_basal` | Plus élevé — à valider |
| P4 | Tests unitaires ciblés sur bootstrap + shadow flat (golden logs ou snapshots limités) | Dépend de la faisabilité dans le module |

## Hors scope immédiat

- Modifier les moteurs ML, l’advisor async, ou les garde-fous safety.
- Ajouter des dépendances inter-modules sans accord.

## Validation recommandée

- `:plugins:aps:compileFullDebugKotlin --no-daemon`
- Smoke test runtime : un cycle loop complet avec comparateur off, vérifier logs et décision SMB/basal attendus.
- Smoke T3c : brittle **on** vs **off** ; avec mode repas / prébolus legacy actif, vérifier que l’ordre therapy → legacy → T3c n’a pas régressé (logs garde prébolus).
- Smoke Meal Advisor : déclencheur explicite (Snap&Go) vs passif (carbs estimés + `modesCondition` true) ; avec **mode legacy** actif, vérifier skip auto et/ou chemin explicite selon attente ; après apply, confirmer qu’aucun SMB aval ne modifie `rT`.
- Smoke Autodrive V3 : préf **active**, conditions **engage** vs **fallback Classic** (commande unsafe) ; vérifier lockout V2 quand `v3AppliedAction` et logs `AUTODRIVE_V3_LOCKOUT`.
